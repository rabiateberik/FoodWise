
@using FoodWise.Web.Helpers
@model List<FoodWise.Web.ViewModels.Stock.StockItemViewModel>

@{
    Layout = "_FoodWiseLayout";

    ViewData["Title"] = "Riskli Ürünler";
    ViewData["PageDescription"] = "Son kullanma tarihi yaklaşan veya israf riski yüksek ürünlerini buradan takip edebilirsin.";
}

@await Html.PartialAsync("_StockTabs")

@section TopbarActions {
    <a asp-controller="Stock" asp-action="Index" class="btn-secondary">
        ← Tüm Stoklara Dön
    </a>
}

<section class="risk-page">
    @if (Model == null || !Model.Any())
    {
        <div class="empty-state">
            <div class="empty-icon">🌿</div>

            <h3>Şu anda riskli ürün yok.</h3>
            <p>Harika! Stoklarında yüksek veya kritik riskli ürün görünmüyor.</p>

            <a asp-controller="Stock" asp-action="Index" class="btn-primary">
                Stokları Gör
            </a>
        </div>
    }
    else
    {
        <div class="risk-summary">
            <div>
                <span>Riskli Ürün</span>
                <strong>@Model.Count</strong>
            </div>

            <div>
                <span>Kritik</span>
                <strong>@(Model.Count(x => x.RiskLevel == "Critical"))</strong>
            </div>

            <div>
                <span>Yüksek Risk</span>
                <strong>@(Model.Count(x => x.RiskLevel == "High"))</strong>
            </div>
        </div>

        <div class="risk-grid">
            @foreach (var item in Model)
            {
                var riskClass = item.RiskLevel switch
                {
                    "Critical" => "risk-critical",
                    "High" => "risk-high",
                    "Medium" => "risk-medium",
                    "Low" => "risk-low",
                    _ => "risk-none"
                };

                var remainingDays = (item.ExpirationDate.Date - DateTime.Today).Days;

                string dayText;

                if (remainingDays < 0)
                {
                    dayText = "Süresi geçti";
                }
                else if (remainingDays == 0)
                {
                    dayText = "Bugün son gün";
                }
                else if (remainingDays == 1)
                {
                    dayText = "1 gün kaldı";
                }
                else
                {
                    dayText = $"{remainingDays} gün kaldı";
                }

                <article class="risk-card @riskClass">
                    <div class="risk-card-top">
                        <div class="risk-title-area">
                            <h3>@item.ProductName</h3>
                            <p>@item.Quantity.ToString("0.##") @DisplayTextHelper.GetUnitText(item.UnitName)</p>
                        </div>

                        <span class="risk-badge @riskClass">
                            @DisplayTextHelper.GetRiskLevelText(item.RiskLevel)

                            @if (item.RiskScore.HasValue)
                            {
                                <span class="risk-score-separator">·</span>
                                <span>@item.RiskScore</span>
                            }
                    </span>
                </div>

                <div class="risk-time-box">
                    <div>
                        <span>Son Kullanma</span>
                        <strong>@item.ExpirationDate.ToString("dd.MM.yyyy")</strong>
                    </div>

                    <p>@dayText</p>
                </div>

                <div class="risk-info">
                    <div>
                        <span>Saklama</span>
                        <strong>@DisplayTextHelper.GetStorageConditionText(item.StorageCondition)</strong>
                    </div>

                    <div>
                        <span>Açılma</span>
                        <strong>
                            @(item.OpenedDate.HasValue
                                                    ? item.OpenedDate.Value.ToString("dd.MM.yyyy")
                                                    : "Açılmadı")
                    </strong>
                </div>
            </div>

            @if (!string.IsNullOrWhiteSpace(item.RecommendationText))
                    {
                        <div class="recommendation">
                            <span>Öneri</span>
                            <p>@item.RecommendationText</p>
                        </div>
                    }

                    @if (!string.IsNullOrWhiteSpace(item.Note))
                    {
                        <p class="note">
                            <strong>Not:</strong> @item.Note
                        </p>
                    }

                    <div class="card-actions">
                        <a asp-controller="Recipe"
                           asp-action="Recommendations"
                           asp-route-stockItemId="@item.Id"
                           asp-route-productName="@item.ProductName"
                           class="action-btn recipe-btn">
                            Tarif Önerisi
                        </a>

                        @if (item.HasActiveShareListing)
                        {
                            <a asp-controller="Sharing"
                               asp-action="MyListings"
                               asp-fragment="listing-@item.ActiveShareListingId"
                               class="action-btn shared-btn">
                                Paylaşımda
                            </a>
                        }
                        else
                        {
                            <a asp-controller="Sharing"
                               asp-action="Create"
                               asp-route-stockItemId="@item.Id"
                               asp-route-productName="@item.ProductName"
                               class="action-btn share-btn">
                                Paylaşıma Aç
                            </a>
                        }
                    </div>
                </article>
            }
        </div>
    }
</section>


@section Styles {
    <style>
        /*
                    Riskli Ürünler sayfası özel düzeni.
                    Kartlar flex-wrap ile yan yana dizilir, alan yetmeyince alt satıra geçer.
                */

        .topbar + .stock-tabs {
            margin-top: 94px !important;
        }

        .stock-tabs {
            gap: 9px;
            margin-bottom: 18px;
        }

        .stock-tab {
            min-height: 38px;
            padding: 9px 15px;
            border-radius: 999px;
            font-size: 13.5px;
            font-weight: 800;
        }

        .risk-page,
        .risk-page *,
        .risk-page *::before,
        .risk-page *::after {
            box-sizing: border-box;
        }

        .risk-page {
            width: 100%;
            display: flex;
            flex-direction: column;
            gap: 18px;
        }

        .risk-summary {
            width: 100%;
            max-width: 760px;
            display: grid;
            grid-template-columns: repeat(3, minmax(0, 1fr));
            gap: 13px;
            align-items: stretch;
        }

            .risk-summary div {
                background: #ffffff;
                border-radius: 15px;
                padding: 14px 16px;
                box-shadow: 0 8px 20px rgba(0, 0, 0, 0.045);
            }

            .risk-summary span {
                display: block;
                color: #607d8b;
                font-size: 12.5px;
                margin-bottom: 5px;
            }

            .risk-summary strong {
                color: #145c24;
                font-size: 25px;
                font-weight: 900;
                line-height: 1;
            }

        .risk-grid {
            width: 100%;
            display: flex;
            flex-wrap: wrap;
            gap: 20px;
            align-items: stretch;
        }

        .risk-card {
            flex: 1 1 calc((100% - 40px) / 3);
            max-width: calc((100% - 40px) / 3);
            min-width: 300px;
            background: #ffffff;
            border-radius: 18px;
            padding: 16px;
            box-shadow: 0 8px 22px rgba(0, 0, 0, 0.05);
            border-top: 4px solid #e5e7eb;
            display: flex;
            flex-direction: column;
            overflow: hidden;
            transition: transform 0.15s ease, box-shadow 0.15s ease;
        }

            .risk-card:hover {
                transform: translateY(-2px);
                box-shadow: 0 12px 26px rgba(0, 0, 0, 0.07);
            }

            .risk-card.risk-critical {
                border-top-color: #dc2626;
            }

            .risk-card.risk-high {
                border-top-color: #f97316;
            }

            .risk-card.risk-medium {
                border-top-color: #eab308;
            }

            .risk-card.risk-low {
                border-top-color: #22c55e;
            }

        .risk-card-top {
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
            gap: 12px;
            margin-bottom: 12px;
        }

        .risk-title-area {
            min-width: 0;
        }

        .risk-card-top h3 {
            color: #145c24;
            font-size: 19px;
            font-weight: 850;
            margin: 0 0 4px;
            line-height: 1.25;
        }

        .risk-card-top p {
            margin: 0;
            color: #526b7a;
            font-weight: 800;
            font-size: 13.5px;
        }

        .risk-badge {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            gap: 4px;
            padding: 6px 10px;
            border-radius: 999px;
            font-size: 12px;
            font-weight: 900;
            white-space: nowrap;
            flex-shrink: 0;
        }

        .risk-score-separator {
            opacity: 0.75;
            margin: 0 1px;
        }

        .risk-badge.risk-critical {
            background: #fee2e2;
            color: #991b1b;
        }

        .risk-badge.risk-high {
            background: #ffedd5;
            color: #9a3412;
        }

        .risk-badge.risk-medium {
            background: #fef9c3;
            color: #854d0e;
        }

        .risk-badge.risk-low {
            background: #dcfce7;
            color: #166534;
        }

        .risk-badge.risk-none {
            background: #e5e7eb;
            color: #374151;
        }

        .risk-time-box {
            background: #f8faf8;
            border-radius: 13px;
            padding: 12px 13px;
            margin-bottom: 10px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            gap: 10px;
        }

            .risk-time-box span,
            .risk-info span,
            .recommendation span {
                display: block;
                color: #6b7280;
                font-size: 12px;
                margin-bottom: 4px;
            }

            .risk-time-box strong {
                display: block;
                color: #1f2937;
                font-size: 15px;
                line-height: 1.3;
            }

            .risk-time-box p {
                margin: 0;
                color: #145c24;
                font-weight: 900;
                font-size: 13px;
                text-align: right;
                white-space: nowrap;
            }

        .risk-info {
            display: grid;
            grid-template-columns: repeat(2, minmax(0, 1fr));
            gap: 9px;
            margin-bottom: 10px;
        }

            .risk-info div {
                background: #f8faf8;
                border-radius: 13px;
                padding: 11px 12px;
                min-width: 0;
            }

            .risk-info strong {
                color: #1f2937;
                font-size: 13px;
                line-height: 1.35;
                word-break: break-word;
            }

        .recommendation {
            background: #f0f8f2;
            border-left: 4px solid #238636;
            border-radius: 13px;
            padding: 11px 12px;
            margin-top: 4px;
        }

            .recommendation p {
                color: #405a68;
                line-height: 1.45;
                margin: 0;
                font-size: 13px;
            }

        .note {
            background: #fff7ed;
            color: #9a3412;
            border-radius: 13px;
            padding: 10px 12px;
            font-size: 13px;
            line-height: 1.45;
            margin: 10px 0 0;
        }

        .card-actions {
            display: grid;
            grid-template-columns: repeat(2, minmax(0, 1fr));
            gap: 9px;
            margin-top: 14px;
        }

        .action-btn {
            min-height: 38px;
            border-radius: 11px;
            padding: 9px 10px;
            font-size: 13px;
            font-weight: 900;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            text-align: center;
            transition: transform 0.15s ease, background 0.15s ease, color 0.15s ease;
        }

            .action-btn:hover {
                transform: translateY(-1px);
            }

        .recipe-btn {
            background: #238636;
            color: #ffffff;
        }

            .recipe-btn:hover {
                background: #1f7a30;
                color: #ffffff;
            }

        .share-btn {
            background: #ecfdf5;
            color: #166534;
            border: 1px solid #bbf7d0;
        }

            .share-btn:hover {
                background: #dcfce7;
                color: #14532d;
            }

        .shared-btn {
            background: #dbeafe;
            color: #1d4ed8;
            border: 1px solid #bfdbfe;
        }

            .shared-btn:hover {
                background: #bfdbfe;
                color: #1e40af;
            }

        .empty-state {
            background: #ffffff;
            border-radius: 18px;
            padding: 44px 22px;
            text-align: center;
            box-shadow: 0 10px 24px rgba(0, 0, 0, 0.05);
        }

        .empty-icon {
            width: 60px;
            height: 60px;
            border-radius: 19px;
            background: #dcfce7;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 30px;
            margin: 0 auto 15px;
        }

        .empty-state h3 {
            color: #145c24;
            font-size: 22px;
            font-weight: 850;
            margin: 0 0 10px;
        }

        .empty-state p {
            color: #526b7a;
            margin-bottom: 20px;
            line-height: 1.5;
            font-size: 14px;
        }

        @@media (max-width: 1350px) {
            .risk-card {
                flex-basis: calc((100% - 20px) / 2);
                max-width: calc((100% - 20px) / 2);
            }
        }

        @@media (max-width: 900px) {
            .risk-summary {
                grid-template-columns: repeat(3, minmax(0, 1fr));
                max-width: none;
            }

            .risk-card {
                flex-basis: 100%;
                max-width: 100%;
            }
        }

        @@media (max-width: 760px) {
            .topbar + .stock-tabs {
                margin-top: 0 !important;
            }

            .risk-summary {
                grid-template-columns: 1fr;
            }

            .risk-card-top,
            .risk-time-box {
                flex-direction: column;
                align-items: flex-start;
            }

                .risk-time-box p {
                    text-align: left;
                }

            .risk-info {
                grid-template-columns: 1fr;
            }

            .card-actions {
                grid-template-columns: 1fr;
            }
        }
    </style>
}







