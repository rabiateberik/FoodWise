@model FoodWise.Web.ViewModels.Stock.EditStockItemViewModel

@{
    Layout = "_FoodWiseLayout";

    ViewData["Title"] = "Stok Düzenle";
    ViewData["PageDescription"] = "Stok ürününün miktar, tarih, saklama koşulu ve not bilgilerini buradan güncelleyebilirsin.";
}

@*
    Stok Düzenle sayfası ortak FoodWise layout yapısını kullanır.
    Kullanıcı ürün adını değiştirebilir; ürün sistemde yoksa API tarafında yeni ürün olarak oluşturulabilir.
    Bu sayfada sadece stok düzenleme formu ve sayfaya özel stiller bulunur.
*@

@section TopbarActions {
    <a asp-controller="Stock" asp-action="Index" class="btn-secondary">
        Geri Dön
    </a>
}

<section class="stock-edit-layout">
    <div class="form-card">
        <div class="form-header">
            <span class="form-icon">✏️</span>

            <div>
                <h3>Stok Ürününü Düzenle</h3>
                <p>Ürün adı, miktar, tarih ve saklama bilgilerini güncelleyebilirsin. Değişiklikten sonra risk durumu yeniden hesaplanır.</p>
            </div>
        </div>

        <form asp-controller="Stock" asp-action="Edit" method="post">
            @Html.AntiForgeryToken()

            @* Güncellenecek stok ürününün Id değeri gizli olarak gönderilir. *@
            <input asp-for="Id" type="hidden" />

            @* Mevcut ürün seçiliyse ProductId bilgisi gizli olarak korunur. *@
            <input asp-for="ProductId" type="hidden" />

            @* Model seviyesindeki doğrulama hataları burada gösterilir. *@
            <div asp-validation-summary="ModelOnly" class="validation-summary"></div>

            <div class="form-grid">
                <div class="form-group product-group">
                    <label asp-for="ProductName"></label>

                    <input asp-for="ProductName"
                           list="productSuggestions"
                           placeholder="Örn: Domates, Avokado, Yoğurt" />

                    <datalist id="productSuggestions">
                        @foreach (var product in Model.Products)
                        {
                            <option value="@product.Text"></option>
                        }
                    </datalist>

                    <small class="field-hint">
                        Ürün listede yoksa yazdığın adla yeni ürün olarak kaydedilir.
                    </small>

                    <span asp-validation-for="ProductName"></span>
                </div>

                <div class="form-group">
                    <label asp-for="UnitId"></label>

                    <select asp-for="UnitId" asp-items="Model.Units">
                        <option value="">Birim seçiniz</option>
                    </select>

                    <span asp-validation-for="UnitId"></span>
                </div>

                <div class="form-group">
                    <label asp-for="Quantity"></label>

                    <input asp-for="Quantity" type="number" step="0.01" placeholder="Örn: 2" />

                    <span asp-validation-for="Quantity"></span>
                </div>

                <div class="form-group">
                    <label asp-for="StorageCondition"></label>

                    <select asp-for="StorageCondition" asp-items="Model.StorageConditions">
                        <option value="">Saklama koşulu seçiniz</option>
                    </select>

                    <span asp-validation-for="StorageCondition"></span>
                </div>

                <div class="form-group">
                    <label asp-for="ExpirationDate"></label>

                    <input asp-for="ExpirationDate" type="date" />

                    <span asp-validation-for="ExpirationDate"></span>
                </div>

                <div class="form-group">
                    <label asp-for="OpenedDate"></label>

                    <input asp-for="OpenedDate" type="date" />

                    <small class="field-hint">
                        Ürün açılmadıysa boş bırakabilirsin.
                    </small>

                    <span asp-validation-for="OpenedDate"></span>
                </div>
            </div>

            <div class="form-group full">
                <label asp-for="Note"></label>

                <textarea asp-for="Note"
                          rows="4"
                          placeholder="İsteğe bağlı not ekleyebilirsin. Örn: Paketi açıldı, yarısı kullanıldı..."></textarea>

                <span asp-validation-for="Note"></span>
            </div>

            <div class="form-actions">
                <a asp-controller="Stock" asp-action="Index" class="btn-cancel">
                    Vazgeç
                </a>

                <button type="submit" class="btn-save">
                    Değişiklikleri Kaydet
                </button>
            </div>
        </form>
    </div>

    <aside class="helper-card">
        <h3>Düzenleme Notları</h3>

        <div class="helper-item">
            <strong>Ürün adı değişirse</strong>
            <p>Yazdığın ürün sistemde varsa mevcut ürün kullanılır. Yoksa yeni ürün olarak eklenir.</p>
        </div>

        <div class="helper-item">
            <strong>Risk tekrar hesaplanır</strong>
            <p>Tarih, saklama koşulu veya ürün bilgisi değiştiğinde israf riski yeniden güncellenir.</p>
        </div>

        <div class="helper-item warning">
            <strong>Paylaşım kontrolü</strong>
            <p>Ürün aktif bir paylaşım ilanındaysa silme işlemi engellenir. Önce paylaşım ilanını iptal etmelisin.</p>
        </div>
    </aside>
</section>

@section Styles {
    <style>
        .stock-edit-layout {
            display: grid;
            grid-template-columns: minmax(0, 1fr) 340px;
            gap: 24px;
            align-items: start;
        }

        .form-card,
        .helper-card {
            background: white;
            border-radius: 22px;
            box-shadow: 0 12px 30px rgba(0, 0, 0, 0.06);
        }

        .form-card {
            padding: 32px;
        }

        .form-header {
            display: flex;
            align-items: center;
            gap: 16px;
            background: #f0f8f2;
            border-radius: 18px;
            padding: 18px 20px;
            margin-bottom: 28px;
        }

        .form-icon {
            width: 54px;
            height: 54px;
            border-radius: 16px;
            background: #dcfce7;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 28px;
            flex-shrink: 0;
        }

        .form-header h3 {
            margin: 0 0 6px;
            color: #145c24;
            font-size: 24px;
        }

        .form-header p {
            margin: 0;
            color: #526b7a;
            line-height: 1.5;
        }

        .form-grid {
            display: grid;
            grid-template-columns: repeat(2, minmax(0, 1fr));
            gap: 20px;
        }

        .form-group {
            display: flex;
            flex-direction: column;
            gap: 8px;
            margin-bottom: 20px;
        }

            .form-group.full {
                margin-top: 4px;
            }

        .product-group {
            grid-column: 1 / -1;
        }

        label {
            font-weight: 800;
            color: #1f2937;
            font-size: 14px;
        }

        input,
        select,
        textarea {
            border: 1px solid #d1d5db;
            background: #ffffff;
            border-radius: 12px;
            padding: 14px 15px;
            font-size: 15px;
            outline: none;
            box-sizing: border-box;
            width: 100%;
            transition: border-color 0.2s ease, box-shadow 0.2s ease, background 0.2s ease;
        }

            input:focus,
            select:focus,
            textarea:focus {
                border-color: #238636;
                box-shadow: 0 0 0 4px rgba(35, 134, 54, 0.12);
                background: #fbfffc;
            }

        textarea {
            resize: vertical;
            min-height: 110px;
        }

        .field-hint {
            color: #6b7280;
            font-size: 13px;
            line-height: 1.4;
        }

        .form-group span {
            color: #dc2626;
            font-size: 13px;
        }

        .validation-summary {
            background: #fee2e2;
            color: #991b1b;
            padding: 14px 18px;
            border-radius: 14px;
            margin-bottom: 22px;
            border-left: 5px solid #dc2626;
        }

            .validation-summary:empty {
                display: none;
            }

        .form-actions {
            display: flex;
            justify-content: flex-end;
            align-items: center;
            gap: 12px;
            margin-top: 10px;
        }

        .btn-save,
        .btn-cancel {
            border: none;
            border-radius: 12px;
            padding: 13px 20px;
            font-weight: 800;
            text-decoration: none;
            cursor: pointer;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            min-width: 170px;
            box-sizing: border-box;
        }

        .btn-save {
            background: #238636;
            color: white;
        }

            .btn-save:hover {
                background: #1f7a30;
            }

        .btn-cancel {
            background: #eef2f4;
            color: #405a68;
        }

            .btn-cancel:hover {
                background: #e2e8ec;
            }

        .helper-card {
            padding: 26px;
            position: sticky;
            top: 24px;
        }

            .helper-card h3 {
                color: #145c24;
                font-size: 22px;
                margin: 0 0 18px;
            }

        .helper-item {
            background: #f8faf8;
            border-radius: 16px;
            padding: 16px;
            margin-bottom: 14px;
        }

            .helper-item.warning {
                background: #fff7ed;
            }

            .helper-item strong {
                display: block;
                color: #1f2937;
                margin-bottom: 6px;
            }

            .helper-item.warning strong {
                color: #9a3412;
            }

            .helper-item p {
                color: #526b7a;
                line-height: 1.5;
                margin: 0;
                font-size: 14px;
            }

            .helper-item.warning p {
                color: #9a3412;
            }

        @@media (max-width: 1100px) {
            .stock-edit-layout {
                grid-template-columns: 1fr;
            }

            .helper-card {
                position: static;
            }
        }

        @@media (max-width: 800px) {
            .form-card {
                padding: 22px;
            }

            .form-header {
                align-items: flex-start;
            }

            .form-grid {
                grid-template-columns: 1fr;
            }

            .product-group {
                grid-column: auto;
            }

            .form-actions {
                flex-direction: column-reverse;
                align-items: stretch;
            }

            .btn-save,
            .btn-cancel {
                width: 100%;
            }
        }
    </style>
}